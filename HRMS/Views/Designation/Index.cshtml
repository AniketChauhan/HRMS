@model IEnumerable<HRMS.Models.HRMS_DESG_MS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@if (Model != null)
{

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Desg_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Desg_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Desg_Short_Name)
            </th>
            <th>
                Parent Department
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>

            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            if (item.IsActive == true)
            {
                if (item.Desg_Parent_ID == null)
                {

                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Short_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Parent_ID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.IsActive)
                        </td>
                        <td>
                            @Html.ActionLink("Edit_Parent", "Edit_Parent", new { id = item.Desg_Id })
                            @Html.ActionLink("Delete_Parent", "Delete_Parent", new { id = item.Desg_Id })
                            @Html.ActionLink("Details", "Details", new { id = item.Desg_Id })
                        </td>
                    </tr>
                }
                else
                {

                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Id)
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Short_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Desg_Parent_ID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.IsActive)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.Desg_Id })
                            @Html.ActionLink("Delete", "Delete", new { id = item.Desg_Id })
                            @Html.ActionLink("Details", "Details", new { id = item.Desg_Id })
                        </td>
                    </tr>
                }
            }
        }

    </table>
}
else
{
    <div class="container">
        <h2 class="text-center text-primary">sorry!!! We don't have any Data Right Now...</h2>
    </div>
}
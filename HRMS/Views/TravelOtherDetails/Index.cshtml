@model IEnumerable<HRMS.Models.HRMS_TRAVEL_OTHER_DETAILS_MS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Travel_Other_Details_NM)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Travel_Other_Details_NM)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Travel_Other_Details_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.Travel_Other_Details_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Travel_Other_Details_ID })
            </td>
        </tr>
    }

</table>
